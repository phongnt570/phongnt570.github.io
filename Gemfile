source "https://rubygems.org"

# Local development only. GitHub Pages ignores this Gemfile and builds
# with its own pinned Jekyll 3.9 + plugins. We use Jekyll 4 locally
# because it runs on modern Ruby; output for this site is identical to
# what GitHub will produce.
gem "jekyll", "~> 4.3"
